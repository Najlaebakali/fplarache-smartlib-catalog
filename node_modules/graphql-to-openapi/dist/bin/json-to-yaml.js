"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var yaml_1 = require("yaml");
var commander_1 = require("commander");
var fs_1 = require("fs");
var filename = commander_1.program
    .description([
    'converts a json file to yaml,',
    'replacing the .json extension with ',
    '.yaml',
].join('\n'))
    .requiredOption('-f, --filename <filename>', 'Input/Output filename')
    .parse(process.argv)
    .opts().filename;
var original = JSON.parse((0, fs_1.readFileSync)(filename).toString());
var output = (0, yaml_1.stringify)(original, { sortMapEntries: true });
(0, fs_1.writeFileSync)(filename.replace(/\.json$/, '.yaml'), output);
