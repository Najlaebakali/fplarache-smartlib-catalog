"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var stringify = require("json-stable-stringify");
var commander_1 = require("commander");
var fs_1 = require("fs");
var filename = commander_1.program
    .description([
    'stable stringifies an input json file,',
    'replacing the input file location with the',
    'new stable stringified contents',
].join('\n'))
    .requiredOption('-f, --filename <filename>', 'Input/Output filename')
    .parse(process.argv)
    .opts().filename;
var original = JSON.parse((0, fs_1.readFileSync)(filename).toString());
var output = stringify(original, { space: '  ' });
(0, fs_1.writeFileSync)(filename, output);
